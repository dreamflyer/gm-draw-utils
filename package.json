{
  "name": "gm-draw-utils",
  "version": "0.0.4",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/dreamflyer/gm-draw-utils.git"
  },
  "scripts": {
    "compile": "rimraf dist && tsc",
    "build": "npm install && typings install && npm run compile"
  },
  "dependencies": {
    "typescript-compiler": "1.4.1-2",
    "geo-math-utils": "*"
  },
  "typings": "./dist/index.d.ts",
  "devDependencies": {
    "typings": "^0.5.1",
    "typescript": "1.8.7",
    "rimraf": "*"
  }
}
